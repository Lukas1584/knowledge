{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug C++(LLDB)",
      "type": "lldb",
      "request": "launch",
      "program": "${command:cmake.launchTargetPath}",
      "cwd": "${workspaceFolder}",
      "args": [],
      "preLaunchTask": "CMake: build"
    }
  ]
}
